{
  "name": "opencms",
  "private": true,
  "packageManager": "bun@1.4.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd apps/web dev",
    "dev:cli": "bun run --cwd apps/cli dev",
    "test": "bun run --cwd packages/db test && bun run --cwd packages/sdk test && bun run --cwd apps/web test && bun run --cwd apps/cli test",
    "build": "bun run --cwd apps/web build",
    "db:generate": "bun run --cwd packages/db db:generate",
    "db:migrate": "bun run --cwd packages/db db:migrate",
    "db:push": "bun run --cwd packages/db db:push",
    "check": "bun run lint && bun run test && bun run typecheck && bun run build",
    "smoke:production": "bun run scripts/production-smoke.ts",
    "typecheck": "bun run --cwd packages/db typecheck && bun run --cwd packages/sdk typecheck && bun run --cwd apps/web typecheck && bun run --cwd apps/cli typecheck && bunx tsc --noEmit -p scripts/tsconfig.json",
    "lint": "bun run --cwd apps/web lint"
  },
  "engines": {
    "bun": ">=1.4.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.13.10",
    "typescript": "^5.8.3"
  }
}
